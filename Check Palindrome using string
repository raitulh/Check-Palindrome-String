#include<iostream>
using namespace std;
int main() {
    string s;
    cin >> s;
    string rev = s;
    reverse(rev.begin(), rev.end());
    if(s == rev)
        cout << "Palindrome\n";
    else
        cout << "Not Palindrome\n";
}
